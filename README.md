# 基于Rust的座位表生成器

## 规则：

1. 座位被分为四个区域：1、2a、2b、3
2. 区域1最多连续坐一次；区域2a与2b分别最多连续坐一次，且一共最多连续坐两次；区域3无限制

## 使用方法：

1. 将座位表模板命名为template.xlsx，并且在对应区域内的每个单元格分别填入1、2a、2b、3
2. 将最近一次历史座位表命名为history1.xlsx，第二近的历史座位表命名为history2.xlsx
3. 运行程序，生成新的座位表至output.xlsx
